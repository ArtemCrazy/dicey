# September layout integration — released 2026-09-15

Production: https://daysi.ru/

Code: 57eda88 (main, pushed). Live-source preservation snapshot: ad1b162.
Both production and the existing preview theme were updated. No credentials,
payment-terminal settings, plugins or production orders were changed.

## Delivered

- Preserved Ivan's live logos, styles, energy-value field and product layout.
- Replaced the logged-in footer demonstration with real template integration.
- Added accessible 3-day / 5-day / month controls to home, shop and related cards.
  Each control updates that card's price, variation and submitted menu selection.
- Added monthly explanation and “Соберите свой рацион” heading; retained the
  existing menu constructor, replacement logic and “В корзину” action.
- Restored menu-based product prices when WooCommerce loads cart sessions,
  before a normal checkout GET. Independent cart lines use cloned product data.
- Connected the supplied payment-button layout to existing gateway ID tbank.
  Removed visible payment selection and the pay-later default from this form.
  When T-Bank is unavailable the form explains this and disables payment.
- Restored the missing SVG checkmark in checkout consent.
- Put Yorkshire terrier first and “Другая порода” last in the breed chooser.
- Kept Moscow-only city/map behavior and corrected stale checkout city handling.
  Replaced font-based delivery markers with CSS circles.
- Updated WordPress delivery page 26: removed the outdated Leningrad-region
  delivery sentence and corrected “Московкой” to “Московской”. Other client
  content, legal company address and the disabled SPB map data were preserved.

## Verification

- 17 isolated PHP regressions in tests/menu-commerce.php passed:
  three/five/month prices, replacement, session restoration, independent
  periods, quantity, repeated calculation, invalid payloads and gateway fallback.
- Touched PHP syntax and JavaScript syntax checks passed.
- In-app Browser, production desktop and 390px mobile:
  - Before: month line 2,880 RUB, total 28,800 RUB.
  - After: month line 28,800 RUB; three-day line 2,880 RUB;
    combined basket and checkout 31,680 RUB.
  - Five-day listing selection: 4,800 RUB; month: 28,800 RUB.
  - Homepage period control: seafood 3-day 2,400 RUB -> 5-day 4,000 RUB.
  - Month detail switches to five days and displays its explanation.
  - Mobile checkout images remain 84 x 75, without stretching.
  - Consent is visibly checked (SVG opacity 1); clicking unchecks it
    (checked=false, SVG opacity 0 after transition).
  - Pay button enabled, no payment radio controls.
  - Only shipping-map-moscow rendered; orange and blue markers visible.
- Live WordPress editor readback matched every deployed file.
- WordPress delivery content was re-read and browser-verified after updating.

## Limits / deferred scope

- No real order, charge, bank redirect or refund was made during these checks.
  Existing bank credentials and availability of bank-side payment products
  were not altered or re-certified.
- iPhone Safari was not physically tested; responsive checks used the in-app
  browser at 390px. Delivery markers no longer depend on Safari's list markers.
- Preview uses older products and has no T-Bank gateway, so final monetary
  and gateway-availability checks were performed on production.
- Footer legal-link rearrangement and designer credit remain deferred per
  the client's September 13 instruction.
- An existing browser tab may retain old HTML; reload / Ctrl+F5 when checking.

## Recovery

Private backups are in .codex/backups (never deployed):
the pre-release theme ZIP dated 20260915-193812 and delivery-content ZIP
dated 20260915-194434. Only the explicit theme-file allowlist is uploaded.
tools/deploy-theme.py checks the expected live baseline and reads files back
after writing; WordPress's editor retains its own PHP rollback protection.
